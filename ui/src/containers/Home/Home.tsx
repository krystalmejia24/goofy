import React from 'react';
import { Props } from './types';

const Home = (props: Props): JSX.Element => {
    return (
        <div>
            goofy!
        </div>
    );
}

export default Home;